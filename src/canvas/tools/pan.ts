import type { ToolHandler } from "../tools";

export const panTool: ToolHandler = {
    onMouseDown(x, y, ctx) {
        
    },

    onMouseMove(x, y, ctx) {
        
    },

    onMouseUp(ctx) {
        
    },
}